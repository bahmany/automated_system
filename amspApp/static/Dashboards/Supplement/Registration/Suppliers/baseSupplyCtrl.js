'use strict';

angular.module('Supplement').controller(
    'baseSupplyCtrl',
    function ($scope,
              $q,
              $http,
              $state,
              $location,
              $rootScope,
              $timeout) {





    });