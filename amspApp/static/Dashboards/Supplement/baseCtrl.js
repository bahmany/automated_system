'use strict';

angular.module('Supplement').controller(
    'homeCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {


        // checking if authenticated





    });