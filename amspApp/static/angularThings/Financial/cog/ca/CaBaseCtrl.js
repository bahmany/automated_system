'use strict';

angular.module('AniTheme').controller(
    'CaBaseCtrl',
    function ($scope,
              //$translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {




    });


