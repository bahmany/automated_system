'use strict';

angular.module('AniTheme').controller(
    'BaseMarakezCtrl',
    function ($scope,
              //$translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {




    });


