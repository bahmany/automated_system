'use strict';

angular.module('AniTheme').controller(
    'Cog75BaseCtrl',
    function ($scope,
              //$translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {


    });


