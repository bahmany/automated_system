'use strict';

angular.module('AniTheme').controller(
    'RialSaziAsaanBaseCtrl',
    function ($scope,
              //$translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {




    });


