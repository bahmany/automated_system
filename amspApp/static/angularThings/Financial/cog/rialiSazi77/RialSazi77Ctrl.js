'use strict';

angular.module('AniTheme').controller(
    'RialSazi77Ctrl',
    function ($scope,
              //$translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {


    });


