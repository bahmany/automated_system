'use strict';

angular.module('AniTheme').controller(
    'homeCtrl',
    function ($scope,
              // $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {


        // checking if authenticated





    });