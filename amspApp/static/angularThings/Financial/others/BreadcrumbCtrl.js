'use strict';

angular.module('AniTheme').controller(
    'BreadcrumbCtrl',
    function ($scope,
              // $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {






    });