'use strict';

angular.module('AniTheme').controller(
    'HeaderCtrl',
    function ($scope,
              // $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {





    });