'use strict';

angular.module('AniTheme').controller(
    'SideBarCtrl',
    function ($scope,

              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {



    });
