'use strict';

angular.module('AniTheme').controller(
    'TopNavCtrl',
    function ($scope,
              // $translate,
              $q,
              $http,
              // $mdDialog,
              $location,
              $rootScope,
              $timeout) {

        // $scope.saleMali = 0;


    });
