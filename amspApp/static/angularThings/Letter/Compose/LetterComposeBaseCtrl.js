'use strict';

angular.module('AniTheme').controller(
    'LetterComposeBaseCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $rootScope,
              $timeout,
              $compile,
              LetterComposeBaseService) {




    });
