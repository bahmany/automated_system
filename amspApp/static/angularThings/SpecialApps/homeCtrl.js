'use strict';

angular.module('AniTheme').controller(
    'homeCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $state,
              $location,
              $rootScope,
              $timeout) {


        // checking if authenticated




    });