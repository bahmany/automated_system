'use strict';

angular.module('AniTheme').controller(
    'SideBarCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {



    });
