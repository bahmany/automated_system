'use strict';

angular.module('AniTheme').controller(
    'BaseTraceCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $location,
              $rootScope,
              $timeout) {





    });