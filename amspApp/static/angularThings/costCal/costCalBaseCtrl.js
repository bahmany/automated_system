'use strict';


angular.module('AniTheme').controller(
    'CostCalBaseCtrl',
    function ($scope,
              $translate,
              $http,
              $q, $mdDialog,
              $rootScope,
              $modal) {

    })