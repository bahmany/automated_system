'use strict';


angular.module('AniTheme').controller(
    'MaterialReportBaseCtrl',
    function ($scope,
              $translate,
              $http,
              $filter,
              $q,
              $mdDialog,
              $element,
              $rootScope,
              $modal) {


    })