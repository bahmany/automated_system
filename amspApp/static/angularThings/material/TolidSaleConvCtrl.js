'use strict';


angular.module('AniTheme').controller(
    'CtrlTolidSaleConv',
    function ($scope,
              $translate,
              $http, $filter,
              $q, $mdDialog, $element,
              $rootScope, toastConfirm,
              $modal) {

    });