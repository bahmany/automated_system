'use strict';

angular.module('RahsoonApp').controller(
    'forgetCtrl',
    function ($scope,
              $translate,
              $q,
              $http,
              $rootScope,
              $timeout) {




    });